

const initialState = {
  setup: "Generate a joke!",
  punchline: "",
  displayPunchline: false,
  isFetching: false,
  error: ""
};

export const reducer = (state = initialState, action) => {
  switch (action.type) {
    default: 
      return state;
  }
}